from grading.types.SectionMeta import *

# Extracts the given findings for the given section text
# { "findings": list[Finding], "notable_anomalies": str }
def extract_findings(section_text:str, prompt:str) -> dict:
    return
